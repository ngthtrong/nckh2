# Dependencies for model/1706.ipynb
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
Pillow>=10.0
scikit-learn>=1.4
tqdm>=4.66

# PyTorch and image model support.
# For an NVIDIA GPU, install the CUDA-specific PyTorch wheel first; see README.md.
# Example (choose the CUDA index that matches the installed NVIDIA driver):
# python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
torch>=2.4
torchvision>=0.19
onnx>=1.16

# VS Code/Jupyter notebook runtime
jupyter>=1.0
ipykernel>=6.29
